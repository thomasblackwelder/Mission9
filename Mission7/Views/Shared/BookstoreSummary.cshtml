@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

@model Book


<div class="card card-outline-primary m-1 p-1">

    <div class="bg-light p-1">
        <h5>@Model.Title</h5>

        <span class="badge bg-info" style="float:right">
            Category: @Model.Category
        </span>
    </div>

    <ul>
        <li>Publisher: @Model.Publisher</li>
        @if (Model.Price > 15.00)
        {
            <li style="color: orangered"> $@Model.Price</li>
        }
        else
        {
            <li style="color : lawngreen"> $@Model.Price</li>
        }
        <li>ISBN @Model.Isbn</li>
        <li>Page count: @Model.PageCount</li>
        <li>Author: @Model.Author</li>
        <li>Classification: @Model.Classification</li>
    </ul>
    <br />



    <form method="post" asp-page="/AddToCart">

        <input type="hidden" asp-for="@Model.BookId" />
        <input type="hidden" name="returnUrl" value="@ViewContext.HttpContext.Request.PathAndQuery()" />


        <span class="card-text">
            <button type="submit" class="btn btn-success btn-sm float-right">Add to Cart</button>
        </span>
    </form>

</div>